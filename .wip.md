# LocalSync WIP — resume here

**Last pushed commit:** `e6062db` — "Fix real desktop-side crash and log duplication, found and verified live on the actual desktop"
Build number `0.1.0+40`, pushed and CI-confirmed green.

## 2026-08-30 (latest) — Settings "Git bare repo path" icon, still uncommitted, needs your go-ahead

**Uncommitted right now:** `lib/screens/settings_screen.dart` + new `assets/logos/git-icon-stroke.svg`. NOT pushed - explicitly waiting on visual confirmation before pushing, per your ask.

Went through many rounds live on-device to get this one field icon right:
1. Official `git-icon.svg` tinted green -> looked like "green diamond, black branch" (it's one solid path with the branch/node shapes cut out as negative space - tinting it green colours the diamond but the cutouts show the dark background through as black).
2. Generic stroke-only branch glyph (two circles + lines, Feather-style) -> lost the recognisable git-logo shape ("needs to be like git logo... but just the stroke").
3. Traced the *real* git-mark geometry as an outline (fill:none) -> at the actual 10px render size (crammed inside an `Icons.computer` overlay) the detail vanished into an unreadable blob.
4. Dropped the computer-icon combo entirely - now a single 22px glyph, same sizing convention as the other two field icons (`person_outline`, `lan`).
5. "stroke circles need to be solid" -> rebuilt from primitives instead of one compound path: a stroke-only rotated-diamond outline + stroke connector line + **3 solid filled circles** for the branch nodes (that's what "solid" meant - the nodes were hollow ring outlines before).
6. "stroke thinner" -> stroke-width 7 -> 5.
7. **"logo is wrong"** - your last word before stopping for the day, no detail given on what specifically. Do NOT guess and re-render again blind - ask what's wrong with it first (shape? proportions? node placement? still not reading as "git" at a glance?) before touching the SVG again.

Current SVG (`assets/logos/git-icon-stroke.svg`): 78x78 viewBox, rotated rounded-square outline (stroke-width 5) + one curved connector path + three filled `<circle r="6">` nodes, everything `#000000` so `ColorFilter.mode(kGreen, BlendMode.srcIn)` tints it uniformly green in the app.

**Next step:** show a fresh preview (thinner-stroke version) using the established golden-test technique (`SvgPicture.asset` at real 22px size, `debugShowCheckedModeBanner: false` - a stray Flutter debug-banner ribbon caused a "hidden by an erroneous line" false alarm earlier, now known and always suppressed). Get explicit go-ahead, then `git add -A`, bump `pubspec.yaml` build number, commit, push.

**Also queued after that** (user explicitly chose "Auto-install with sudo" when asked): extend the existing git-auto-install-via-sudo mechanism to also check for/install `python3`. The pattern already exists for `git` alone:
- `lib/features/pairing/pairing_controller.dart`'s `pairWithPassword()` - checks `command -v git` over the still-in-memory desktop password during initial pairing (the only window where sudo is possible - all later SSH is key-only), calls `onGitMissing()`, on consent runs `sudo -S apt-get install -y git`, handles exit code 3 (no apt-get / likely macOS) and other failures distinctly.
- `lib/widgets/git_install_consent.dart` - the consent dialog, currently hardcoded to "git" only (title, info lines, displayed command all say "git" specifically).

To extend: broaden the presence check beyond just `command -v git`, genericize the consent dialog's hardcoded text to name whichever package(s) are actually missing, extend the install command to include `python3` when needed. Not started yet.

## 2026-08-30, SSD recovery — drive is failing, dry-run scan abandoned

The read-only `fsck.vfat -n` dry-run on `/dev/sda1` (corrupted 1.8TB FAT32 "Usb_stick") had been running in the background since earlier in the day. Checked progress: **9.5 hours wall-clock, only 3.3GB of 1.8TB read (0.18%)** - at that rate it would take ~200+ days to finish. That's not "slow," that's the drive hardware struggling badly on retries (likely widespread bad sectors). Killed the process, `sync`'d, unmounted and powered off via `udisksctl` - confirmed gone from `lsblk`, safe to physically remove.

**Real implication worth weighing tomorrow**: this reading suggests the drive itself may be too far gone for a straightforward file-level recovery. Worth deciding whether to attempt a slow block-level image (`ddrescue`, which is built for exactly this - skips bad sectors instead of retrying forever, comes back for them last) onto the reclaimed Project Space partitions instead of another plain `fsck`/`rsync` pass, or whether the ~700GB Movies/TV Series sole-copy content is worth that time investment at all. Not decided - see `[[project_backup_system]]` memory for the full partition-reclaim context.

## 2026-08-28, real-device testing marathon - 14 builds, most real bugs fixed, one open item

Full day of live device testing against real builds (not stale-build confusion this time - `pubspec.yaml`'s build number is now bumped every push specifically so this can't recur, see the section below). ~24 commits from `28e78d5` to `980a60d`. In rough chronological order:

**Real, confirmed-fixed bugs:**
- **Personal info leak**: `main.dart` hardcoded this developer's own real desktop username/IP/bare-repo-path as the fallback shown to any fresh install, including the literal `pi5-obsidian` folder name. Blanked - `_needsSettings` already handles the empty case.
- **Raw SocketException on pairing before Settings is set**: blanking those defaults meant PAIR NOW with nothing configured hit `SSHSocket.connect('', ...)` - new `LinkingError.desktopNotConfigured` catches it before any connection attempt, with a real message.
- **All-or-nothing Settings save**: one invalid field (IP with a CIDR `/28` suffix, copied from `ip addr show`'s own output) silently discarded two other valid fields instead of saving what it could. Each field now saves independently.
- **IP field CIDR confusion**: `/nn` suffix now stripped automatically before validating, AND spelled out in the field's own always-visible helper text (not just behind the (i) button) - "must be clear, it's a small ambiguity amongst many."
- **Stale `PairingController` snapshot**: built once in `initState()` with whatever `desktopUser`/`desktopIp` existed at mount time - visiting Settings afterward and coming back to the same still-alive screen never picked up the update. Re-synced fresh immediately before every real pairing attempt now.
- **Suggested-path collision**: "Use suggested path" always suggested the identical path - a second vault would silently no-op via the existing remote-identity dedup check. Now numbers each suggestion per Settings visit (`localsync.git`, `localsync-2.git`, ...).
- **Settings label/hint confusion**: empty fields rested their bold label INSIDE the box (Material default), reading as already-typed content; hint text stayed hidden until first tap. `floatingLabelBehavior: always` on all three fields fixes this globally.
- **Auto-discovery / USB confusion**: satellite-icon search is Wi-Fi/mDNS only: tooltip and the "not found" message now say so and point USB-tether users at the manual command instead of leaving them waiting with no explanation.
- **`docs/desktop-setup.md` stale**: said "nothing automated exists yet" for desktop-side sync - false, `desktop/localsync_sync.sh` has existed since the prior session. Added a real section: copy it over, configure `LOCALSYNC_VAULT`/`LOCALSYNC_BARE_REPO` to match Settings, cron example.
- **App-bar folder-name wrap**: a real long folder name ("Files needed on phone") split awkwardly. A FittedBox(scaleDown) attempt was a real regression (wrong mechanism for a wrapping multi-line block, collapsed it toward one line) - reverted, then fixed properly by dropping the font to 11px so more words fit per line. **Confirmed working now.**

**Reverted after real iteration problems - not worth chasing further today:**
- Sparkle "fade to blue" treatment on the password-field icons (both the inner prefixIcon cluster and the separate outer star at `Positioned(left: -10, ...)`) went through several rounds (wrong timing understanding, then no twinkle, then twinkle amplitude too subtle) and was explicitly called off - "it's out of time." Reverted to plain hard show/hide, keeping only the *correct* stop-condition (`_field1Done`, tied to field 2 gaining focus, not just field 1 being non-empty - that part was confirmed working and stays).

**OPEN, unresolved as of end of session**: user reported "Blue stars fade away still there on passwords fields, to be removed" *after* the revert commit (`980a60d`, build `0.1.0+14`) was already pushed and built green. A grep of the current source turns up no remaining sparkle-fade-to-blue code in `shredding_password_field.dart` or `linking_screen.dart` - the only `kBlue` reference left in that area is the ORIGINAL shred-dissolve animation (the characters-flying-apart effect on actual pairing submit), which is a separate, older feature the user explicitly said "looks great" on earlier in this same session. **Two real possibilities, unconfirmed**: (a) they were looking at a build older than `0.1.0+14` (check the version label at the bottom of Settings before touching code again), or (b) they've changed their mind and now want the ORIGINAL shred-dissolve dots' blue color removed too, not the sparkle-icon fade I'd already reverted. **Do not guess-fix again** - confirm the exact build number shown in Settings, and confirm which element (sparkle icon vs. the password-shredding-into-dots animation on submit) before touching this a third time.

**package_info_plus, device_info_plus**: both real native dependencies, both CI-verified clean across many builds today - no longer a risk to flag.

---

## 2026-08-28, later same day: build-number staleness confusion, resolved

Live device-test round reported several issues (welcome screen still "technical," folder/Vault wording not applied, `pi5-obsidian` path visible, Step 2 password field not auto-focusing, app-bar name text still off) that all traced back to already-merged code (`5d360e6`) - not a code bug. Root cause: `pubspec.yaml`'s build number was never bumped (`0.1.0+1` for every single build produced today), which can make a sideload tool treat a new build as already installed and skip replacing the binary.

**Real fixes this round, commit `3978a25`:**
- `main.dart` - blanked hardcoded personal `desktopUser`/`desktopIp`/`bareRepoPath` defaults (this developer's own real desktop info: `rapi5`, `172.20.10.11`, `.../pi5-obsidian/...`) that were pre-filling Settings on any fresh install, real customer or Tier 0 tester alike. `_needsSettings` (linking_screen.dart Stage 1) already handles the empty-defaults case correctly, so this is the actually-correct default now, not a regression.
- `linking_state.dart` - `bareRepoNotFound`'s resolution text no longer tells the user to check `main.dart` or shows a hardcoded desktop path - now points at Settings.
- `settings_screen.dart` - added a version/build label at the bottom (`package_info_plus`, new native dependency, not yet CI-verified) so which build is actually installed is visible at a glance instead of inferred from git commits after the fact.
- `pubspec.yaml` - build number `0.1.0+1` -> `0.1.0+2`.

**Traced but found no bug in**: the password-autofocus fix (`_onKeyPairingSettled` -> `addPostFrameCallback` -> `_passwordFocusNode.requestFocus()` -> `ShreddingPasswordField` correctly forwards `focusNode` into its real `TextField`) and the app-bar name-centering fix (`_AppBarRepoStatus` in home_screen.dart) - both read correct on paper. Best explanation remains the stale build, not these specific fixes being wrong.

**Next real step**: once Codemagic finishes building `3978a25`, **delete the app completely from the phone** before reinstalling (not just overwrite) - rules out any sideload-tool caching for good, and the new build number should now show up in Settings' new version label. Re-test everything from the 2026-08-27/28 sessions once confirmed on a genuinely fresh install: SyncChoiceScreen -> "Just my files" -> pairing -> password field autofocus -> Settings screen (now-blank defaults, folder/Vault wording, text positioning) -> app-bar name text -> a real conflict on each device -> Undo -> binary-conflict pick screen.

**package_info_plus not yet CI-verified** - same caveat device_info_plus carried when it was first added (2026-08-18): standard, widely-used plugin, lower risk than the git2dart saga, but genuinely unverified until a real Codemagic build completes clean.

---

## Earlier: 2026-08-27 session (superseded by the above for "what's next," kept for history)

**Prior last commit:** `28e78d5` — "Add desktop/localsync_sync.sh - real, tested desktop-side sync automation"

## What happened this session (2026-08-27), in order

1. **Undo for resolved conflicts** - kept text now gets an invisible HTML-comment marker (matching span to the existing reference-callout side), real UNDO button on the Conflicts screen. Tested (6/6 pass).
2. **Real DNS/domain work** (Website repo, not this one) - kworld.space fully live via Cloudflare, site renamed synclocal→LocalSync, pricing rebuilt to the real tier ladder, contact form's real backend bug found and fixed (a stuck quick-tunnel systemd service was silently degrading every "Buy access" flow on the site), same fix applied to defi-rate-proxy. Both now use a stable named-tunnel route instead of a rotating one.
3. **Tier 0 (free, no-PKM file sync) - the whole loop, start to finish:**
   - `Repository.syncMode` (obsidianVault/genericFolder), tested.
   - `LinkingController.startLinkingGenericFolder()` - skips the Obsidian-installed check, reuses the same pairing/clone machinery.
   - **Real safety bug found and fixed**: `repairAllConflictsOnDisk` only ever handled `.md` files - any other conflicted file (binary attachments in an *existing* vault too, not just Tier 0) fell through with zero backup/detection. New `repairBinaryConflictsOnDisk` (sync_service.dart) backs up both sides, keeps "ours". **Not execution-verified locally** - this Pi is arm64, git2dart's bundled test binaries are x86_64-only, `flutter test` can't load them here, and CI never runs `flutter test` at all (only `flutter build ipa`). The fix built clean on the real iOS pipeline though (`a26437e` succeeded).
   - Real pick-either-side UI for whole-file conflicts (`binary_conflict_log.dart` + `BinaryConflictsScreen`) - own JSON log in `Conflict Backups/`, since binary bytes can't carry inline markers.
   - `SyncChoiceScreen` - the actual first screen on a fresh install now ("Just my files" / "My Obsidian notes"), before any PKM/vault UI appears at all. Pairing screen itself made mode-aware too (title, imagery).
   - `PkmSyncUpsell` on a Tier 0 user's home screen - real IAP offer for `kPkmSyncEntitlementId`; on purchase, runs the exact same vault-linking sequence the free chooser's Obsidian path uses. Button stays disabled until a real RevenueCat product exists (no funded Apple Developer account yet - still the $99 blocker).
4. **desktop/localsync_sync.sh** - real desktop-side sync automation, since `docs/desktop-setup.md` admitted "nothing automated exists for it yet." Adapted from the user's own real `~/Documents/Scripts/synco.sh` (years-refined). Extended with the same binary-conflict safety net as the phone side. **Actually tested** (unlike the Dart binary-conflict fix) - real temp git repos, bootstrap clone, push-ahead, and a real diverged merge with BOTH a markdown and a binary conflict in the same run. Found and fixed two real bugs during testing (a `set -e` gotcha where a bare `[[ cond ]] && cmd` as a function's last line silently killed the whole script when cond was false, and a flawed "did anything change vs HEAD" gate that never fires for a binary conflict resolved to "ours"). All scenarios pass now.

## Real next steps, in priority order

1. **Real-device test everything from this session** - none of today's Dart/Flutter work has touched a real phone yet. Sideload the latest build and walk through: fresh install → SyncChoiceScreen → "Just my files" → pairing → a real conflict on each device → Undo → the binary-conflict pick screen.
2. **desktop/localsync_sync.sh still needs testing against a REAL bare repo over real SSH** - today's testing was local temp repos on this same machine, never over the actual desktop↔bare-repo SSH path real users will use.
3. **docs/desktop-setup.md needs updating** - it still says "nothing automated exists for it yet," which is now false. Should document `desktop/localsync_sync.sh` (config via `LOCALSYNC_VAULT`/`LOCALSYNC_BARE_REPO` env vars, or edit the defaults) and probably a cron/systemd-timer suggestion for running it periodically, not just by hand.
4. **Whole-file conflict UI is auto-keep-ours only on the desktop side** - the phone got a real pick-either-side screen; the desktop script always keeps "ours" with no equivalent interactive choice (reasonable for now - a script can't show UI - but worth deciding if that's the permanent design or if desktop conflicts should just wait for the phone user to resolve via `BinaryConflictsScreen` instead).
5. **IAP still can't go live** - blocked on the $99 Apple Developer Program payment. Once paid: create the real App Store products, configure them in the RevenueCat dashboard (matching `kPkmSyncEntitlementId`/`kConflictPickerEntitlementId`), and both upsell buttons come alive with zero further code changes needed.

## Other loose threads, lower priority

- Linking screen copy still says "vault"/"PKM" in a few spots beyond what got fixed today (the Obsidian-specific vault-creation instructional text, deliberately left alone - it has to match Obsidian's real on-screen buttons).
- SSD backup: rsync to the corrupted USB drive was stopped cleanly (`--partial`, safe to resume) and the drive safely unmounted/powered off before the user left today - not resumed, real fix (reformat + fresh copy to a healthy drive) still pending, see `[[project_backup_system]]` memory for full context.
- Domain renewal costs are real and tracked (`[[project_website]]` memory) - kworld.space renews at ~CHF 15.66/year via Spaceship, not urgent.
